import React from "react";
import styles from '../login-layout/splash/splash.module.scss';

const Loading: React.FC = () => (
    <section className={styles.splash}>
        <p className={styles.splash__title}>Tonder</p>
    </section>
);

export default Loading;
